from tensorflow.keras.models import load_model

# Load the model
model = load_model("crop_classifier_model.keras")

# Use it for predictions, evaluation, or further training
# Example: model.evaluate(val_data)

